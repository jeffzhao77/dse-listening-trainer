# 试卷提取工具链 — 使用说明（已端到端验证）

把「完整录音 + 题库 JSON（含逐字稿）」自动变成可导入的试卷。全流程本地 Whisper / 阿里云 paraformer 语音识别，**已用 MT56 四段音频拼接完整录音验证通过**（Task 边界与源音频误差 <0.1s）。

## 一条命令跑通（云端转写，快）

```powershell
cd DSE-Listening-System-M1-M2-code

node scripts/audio-align.js MT56-PartA-学生完整包/data/<卷id>_partA.json <完整录音.mp3 或公网URL> `
  --asr dashscope `
  --api-key <你的 sk-ws- key> `
  --model paraformer-v2 `
  --out data/<卷id>_aligned.json
```

- 本地 mp3 会自动上传 OSS（配置在 `data/.oss-config.json`，已填好你的 AK/bucket）拿签名 URL → 转写 → 对齐
- 输出 `aligned.json`：每句真实文本 ↔ 起止秒 ↔ 说话人 + 每个 Task 的 [start, end] 边界

## 三个工具

| 工具 | 命令 | 产出 |
|---|---|---|
| ① 音频对齐 | `node scripts/audio-align.js <json> <音频>`（`--asr dashscope` 云端 / 默认本地 whisper） | `aligned.json` 集成文件 |
| ② 自动剪裁 | `node scripts/split-audio.js <aligned.json> --audio <完整录音> --prefix <卷id>` | `<卷id>_taskN.mp3` |
| ③ AI 配对 | `node ai-extract/index.js <OCR文本.txt> --provider openai --base-url ... --api-key ... --model qwen3.8-flash` | 题库 JSON |

服务器工具页：启动 `node server/index.js` → 教师端登录 → 顶部「**试卷工具**」→ ⓪ API 自测 / ① 音频对齐 / ② 自动剪裁 / ③ AI 提取。

## 配置（已就绪）

- **`data/.oss-config.json`**：OSS AK/bucket（华北2北京，私有），音频自动上传用 —— 已忽略提交
- **`data/.audio-align-tmp/full.json`**：转写缓存，重跑对齐加 `--skip-asr` 秒出（省额度）
- 转写模型 `paraformer-v2`：你的空间有 **36000 秒免费额度**（`bl usage free` 可查）

## 注意

- OSS 签名 URL 1 小时有效：转写会自动上传并立即提交，无需担心过期
- `--skip-asr` 复用已有转写（同一份音频反复调参时必加）
- 对齐结果分三档：`exact`（可靠）/ `fuzzy`（建议复核）/ `missing`（人工补）
  - MT56 实测（paraformer 转写 + **DP 全局序列对齐**）：**86% 自动对齐**（215 exact + 31 fuzzy / 285 句），各 Task 均在 80%+（含电话短对话场景）
  - `missing` 的句子已附 `near` 字段（最接近的 ASR 句及其时间），人工复核时可快速定位
- 题级 ts（demo 精听所需）：`node scripts/fill-ts.js 题库.json aligned.json` 自动填每题 `ts.signal/ts.answer`
  - MT56 实测 **37/42 题自动命中**，`answer` 平均误差约 3 秒（句子级区间边界差），配合 demo 校准模式（−2s/+2s 微调）30 秒精调一题
  - 少数未命中的题（signal/cloze 文本与逐字稿差异大）需人工在校准页补
- 最后一个 Task 的结束边界取音频物理时长（自动探测），不会切短

## 语言识别 API 开通（一次）

你的 `sk-ws-` key 在**标准百炼**（dashscope.aliyuncs.com）可调 `paraformer-v2`（已验证），语音模型已开通且有免费额度。若额度用完需在 `bailian.console.aliyun.com` 按量付费开通 qwen3-asr 等。

## 实战案例：2021 HKDSE Paper 3 Part A（已跑通 demo 级效果）

素材：扫描版题目卷+评分标准 PDF（77 页，含完整 Tapescript）+ 整卷录音 mp4 → 产出：

- `MT56-PartA-学生完整包/data/2021HKDSE_P3A_partA.json`（56 题，Task 1-4）
- `MT56-PartA-学生完整包/audio/2021HKDSE_task1-4.mp3`（自动按播报词切割）

流程：qwen-vl-ocr 多模态识别扫描卷 → LLM 生成每题 signal/cloze → paraformer 转写 → DP 对齐（87.6%）→ split 剪裁 → fill-ts 填题级 ts（56/56 命中）→ validate 通过。

测试：`http://localhost:8011/partA.html?data=data/2021HKDSE_P3A_partA.json`（加 &teacher=1 校准 ts）
