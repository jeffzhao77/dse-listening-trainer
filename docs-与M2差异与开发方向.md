# 与早期 M2 版本的区别 & 后期开发方向（移交参考）

> 早期版本 = 今天开工前的状态（M1 骨架 + M2 作答持久化 + M3 教师端雏形 + M4 auto-ts 雏形）。
> 本整理版 = 今天完成的全量交付。

## 一、相比早期 M2 版本，新增/变化了什么

### 1. 判分与作答（M1/M2/M3）—— 保留未变
- 学生登录做题、交卷权威判分、教师端报表/导出、demo 演示数据 —— 与早期一致。

### 2. 全新：试卷提取工具链（把 M4 从"雏形"变成"完整落地"）
| 能力 | 早期（M2 时代） | 现在 |
|---|---|---|
| 音频转写 | 仅本地 whisper.cpp（CPU 慢，20-40 分钟/卷） | 云端 paraformer-v2（56 分钟录音约 5-15 分钟），本地保留为备选 |
| 分 Task 剪裁 | 无（Audacity 手工） | `split-audio` 按播报词自动切 mp3（边界误差 <0.1s） |
| 逐字稿对齐 | `auto-ts.js` 词级匹配（短句/合并句易错配） | `audio-align` **DP 全局序列对齐**（句子级，MT56 对齐率 86%，Task 2 从 16%→84%） |
| 题级 ts | 靠 auto-ts 逐题匹配，不稳 | `fill-ts` 从句子时间轴填每题 ts（2021 卷 56/56 命中，误差 ~3s 配合校准） |
| 扫描卷 OCR | 无（人工打字） | `qwen-vl-ocr` 多模态识别（77 页扫描 PDF 全量提取，含答案+逐字稿） |
| AI 题目配对 | 无 | `ai-extract` 独立服务（provider 抽象：mock/openai，可接本地模型） |
| 精听数据（signal/cloze） | 手工从逐字稿抄 | LLM 自动从逐字稿摘录（56/56 题） |

### 3. 全新：云端语音识别基础设施
- OSS 自动上传（签名 URL）、paraformer 异步转写、转写缓存（--skip-asr 复用省额度）
- 阿里云百炼 CLI（bl）接入，可查额度/用量
- `test-api.js` 一键连通性自测

### 4. 全新：2021 真题实战验证
从「扫描 PDF（77 页）+ 整卷录音 mp4」到「可作答/判分/精听的 56 题试卷」全流程跑通，证明工具链对真实新卷可用。

### 5. 工程化与移交
- 项目重组为整理版（server/apps/shared/scripts/ai-extract/papers/data 清晰分层）
- Skill `/dse-voice-recognition-setup` + 移交文档，下一位接手者可快速续配

### 6. 相对早期版本仍然缺失/未完成的部分
- 学生端仍写死 mt56 卷（新卷只能在 demo 页面 partA.html 测，不能在线选卷）
- 前端判分仍是内联复刻，未真正引用 `shared/grade.js`（设计原则"判分同源"未完全落地）
- M5 analytics（raw events + 占位 API）未实现
- 教师端「试卷工具」页 API 已验证，但浏览器交互未完整实测
- 对齐仍有 ~14% 句子需人工补；ts 为句子级区间（词级 refine 未做）

## 二、后期开发方向（按优先级）

### P0（最影响"可用"）
1. **学生端选卷**：试卷列表/按班级指定卷，让 2021 新卷能在线作答（当前只能 demo 页面测）
2. **教师端工具页补完**：OCR 上传、fill-ts 接入页面、浏览器实测；让非技术老师也能操作

### P1（质量与体验）
3. **判分同源落地**：exam.html 改为引用 shared/grade.js，消除两端判分不一致风险
4. **ts 词级 refine**：答案句在句子区间内进一步切到词级，提升精听精度（从 ~3s 到 ~0.5s）
5. **对齐率提升**：剩余 ~14% missing 用更强的容错（同音词表、数字/日期归一化）+ 复核界面

### P2（数据与智能化）
6. **M5 analytics**：raw events 导出 + 占位分析 API（作答流水已落库，只差接口）
7. **M6 AI 错因诊断**：消费作答数据（逐题耗时/精听行为）做薄弱点分析（设计说明书预留方向）

### P3（工程与合规）
8. **语音增强**：qwen3-asr 接入（说话人分离、热词、字级时间戳）
9. **版权合规**：真题访问控制、音频防盗链（设计说明书 §9）
10. **多卷管理**：题库导入工具页化、版本管理
